import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tesseract.wasm.react',
  appName: 'tesseract-wasm-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
