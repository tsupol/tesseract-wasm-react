import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tesseractwasm.react',
  appName: 'tesseract-wasm-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
